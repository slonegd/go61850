# 📦 OSI Levels in IEC 61850

Этот модуль предоставляет реализации различных уровней модели OSI, используемых в коммуникационном стандарте **IEC 61850**, который применяется в автоматизации электрических подстанций. Каждый подпакет представляет собой определённый уровень модели OSI, обеспечивая структурированную и стандартизированную коммуникацию между интеллектуальными электронными устройствами (IED).

## 📚 Подпакеты

### 1. `cotp` — Транспортный уровень (Уровень 4)

**Протоколы:** TPKT + ISO 8073 / X.224 COTP (Connection-Oriented Transport Protocol)

**Назначение:**
- Обеспечивает надёжную, ориентированную на соединение передачу данных между приложениями.
- Обрабатывает сегментацию, управление потоком и восстановление после ошибок.
- Работает поверх TCP (Transport Control Protocol) в большинстве реализаций IEC 61850.
- Гарантирует доставку блоков данных (TPDU) в правильной последовательности без потерь.

**Пример использования в IEC 61850:**
- Используется для установления и поддержания транспортного соединения для сеансов MMS (Manufacturing Message Specification).

---

### 2. `session` — Сеансовый уровень (Уровень 5)

**Протокол:** ISO 8327-1 OSI Session Protocol

**Назначение:**
- Управляет коммуникационными сеансами между приложениями.
- Контролирует координацию диалога (полный/полудуплекс).
- Предоставляет точки синхронизации и обрабатывает установление, обслуживание и завершение сеансов.

**Пример использования в IEC 61850:**
- Устанавливает и управляет сеансами на уровне сессии для MMS поверх транспортного уровня.

---

### 3. `presentation` — Уровень представления (Уровень 6)

**Протокол:** ISO 8823 OSI Presentation Protocol

**Назначение:**
- Определяет синтаксис обмена данными между приложениями.
- Обрабатывает представление данных, шифрование и сжатие (если применимо).
- Обеспечивает, чтобы данные были представлены в формате, понятном прикладному уровню.

**Пример использования в IEC 61850:**
- Определяет, как данные MMS и ACSE кодируются/декодируются с использованием ASN.1 BER (Basic Encoding Rules).

---

### 4. `asce` — Элемент управления ассоциациями (Уровень 7, часть прикладного уровня)

**Протокол:** ISO 8650-1 OSI Association Control Service Element (ACSE)

**Назначение:**
- Управляет установлением, завершением и аутентификацией ассоциаций между приложениями.
- Определяет контексты приложений и представления.
- Выступает в роли механизма рукопожатия для прикладных протоколов высокого уровня, таких как MMS.

**Пример использования в IEC 61850:**
- Инициирует и согласовывает параметры для сеансов связи MMS.

---

### 5. `mms` — Manufacturing Message Specification (Прикладной уровень, Уровень 7)

**Протокол:** MMS (в IEC 61850 используется как основной протокол прикладного уровня)

**Назначение:**
- Обеспечивает стандартизированный интерфейс для коммуникации между IED.
- Поддерживает операции, такие как чтение/запись переменных, управление устройствами, передача статуса и синхронизация времени.
- Реализует сервисы, такие как доступ к переменным, управление доменами, передача файлов и ведение журналов.

**Пример использования в IEC 61850:**
- Используется для обмена данными между системами SCADA, реле защиты и другими IED на подстанции.

---

## 🔄 Взаимодействие уровней в IEC 61850

MMS (Приложение) → ACSE (Ассоциация) → Presentation → Session → COTP (Транспорт) → TCP/IP (Сеть/Транспорт)

Каждый уровень инкапсулирует данные из вышестоящего уровня, добавляя свои заголовки и управляющую информацию, прежде чем передать их следующему нижнему уровню.

В зависимости от типа сообщения, в нём могут присутствовать **разные уровни OSI**:

- **При установлении ассоциации (например, AARQ/AARE):**  
  Все уровни (от `cotp` до `mms`), включая `session` и `asce`.

- **В обычных MMS-операциях (например, Read, Write, Report и т.д.):**  
  Используются уровни `cotp`, `presentation`, `mms`.  
  Уровни `session` и `asce` **отсутствуют**, так как сеанс уже установлен.

Таким образом, не все подпакеты используются в каждом сообщении — структура зависит от типа и этапа взаимодействия.

---

## 📌 Заметки

- Эти уровни OSI реализованы поверх TCP/IP в большинстве современных сред IEC 61850.
- Модуль разработан для поддержки разбора, кодирования и декодирования сообщений IEC 61850 для использования в инструментах и приложениях автоматизации подстанций.

